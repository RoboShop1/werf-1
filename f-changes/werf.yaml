# werf.yaml
project: example
configVersion: 1
---
image: backend
from: alpine
git:
  - add: /docs/sample.txt
    to: /app/sample.txt
    stageDependencies:
      install:
        - app/
shell:
  beforeInstall:
    - mkdir /app
  install:
    - echo "This stage will execute based on changes"
